# Claude repository instructions

Read and follow `AGENTS.md` at the repository root, then the nearest nested
`AGENTS.md` for the files being changed. Durable task state lives in
`.harness/tasks/`, not only in the conversation.

